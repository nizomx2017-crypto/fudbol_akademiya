const Redis=require("ioredis");let client;function getRedis(){if(!client)client=new Redis(process.env.REDIS_URL||"redis://localhost:6379",{lazyConnect:true,maxRetriesPerRequest:1});return client}async function cache(key,ttl,loader){const r=getRedis();if(r.status==="wait")await r.connect();const hit=await r.get(key);if(hit)return JSON.parse(hit);const value=await loader();await r.set(key,JSON.stringify(value),"EX",ttl);return value}module.exports={getRedis,cache};
