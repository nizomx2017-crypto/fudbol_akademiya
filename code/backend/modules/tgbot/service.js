const r=require("./repository");async function send(chatId,text){if(!process.env.TELEGRAM_BOT_TOKEN)return{skipped:true};const response=await fetch(`https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:chatId,text})});return response.json()}module.exports={handle:async update=>{const m=update.message;if(!m)return{ok:true};const text=m.text||"";if(text.startsWith("/start ")){const userId=Number(text.slice(7));if(userId)await r.link(userId,String(m.chat.id));return send(m.chat.id,"Account bog‘landi.")}if(text==="/help")return send(m.chat.id,"Buyruqlar: /start <userId>, /help");return send(m.chat.id,"O‘quv markazi botiga xush kelibsiz.")},notify:async(userId,text)=>{const link=await r.find(userId);return link?send(link.chatId,text):{skipped:true}}};
