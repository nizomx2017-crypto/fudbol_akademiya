const r=require("express").Router(),multer=require("multer"),c=require("./controller"),d=require("./dto"),{AppError}=require("../../shared/errors");const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:d.maxSize},fileFilter:(req,file,cb)=>cb(d.allowedTypes.includes(file.mimetype)?null:new AppError(415,"Fayl turi ruxsat etilmagan","INVALID_FILE_TYPE"),d.allowedTypes.includes(file.mimetype))});r.get("/",c.list);r.post("/",upload.single("file"),c.upload);r.get("/:id/download",c.download);r.delete("/:id",c.remove);module.exports=r;
